# pytest.ini

[pytest]
markers = integration: marks tests as real API integration tests
log_cli = true
log_cli_level = INFO
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %H:%M:%S